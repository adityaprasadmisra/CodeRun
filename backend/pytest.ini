[pytest]
asyncio_mode = auto
testpaths = tests
addopts = -ra -q
markers =
    slow: marks tests that call the real Ollama LLM (may take ~30-60s each)
    integration: marks tests that exercise the DB / HTTP / WebSocket stack end-to-end
filterwarnings =
    ignore::DeprecationWarning
